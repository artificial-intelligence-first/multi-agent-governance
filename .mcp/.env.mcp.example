# Example configuration for enabling the GitHub MCP provider.
# Copy this file to `.mcp/.env.mcp` and replace placeholder values as needed.
MCP_ROUTER_PROVIDER=github
GITHUB_TOKEN=<replace-with-github-token>
# Optional overrides for self-hosted GitHub or custom timeouts.
GITHUB_API_BASE=https://api.github.com
GITHUB_API_VERSION=2022-11-28
GITHUB_TIMEOUT_SEC=15
