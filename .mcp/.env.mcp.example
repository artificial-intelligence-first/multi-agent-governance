# Example configuration for enabling the GitHub MCP provider.
# Copy this file to `.mcp/.env.mcp` and replace placeholder values as needed.
MCP_ROUTER_PROVIDER=github
GITHUB_TOKEN=<replace-with-github-token>
# Optional overrides for self-hosted GitHub or custom timeouts.
GITHUB_API_BASE=https://api.github.com
GITHUB_API_VERSION=2022-11-28
GITHUB_TIMEOUT_SEC=15
# Context7 MCP server (optional but recommended)
CONTEXT7_MCP_URL=https://mcp.context7.com/mcp
CONTEXT7_API_URL=https://context7.com/api/v1
CONTEXT7_API_KEY=<replace-with-context7-api-key>
# Serena MCP server (started via uvx)
SERENA_CONTEXT=ide-assistant
SERENA_PROJECT_ROOT=/abs/path/to/project
# Chrome DevTools MCP server (launches via npx; ensure Chrome + Node 20.19+ are available)
#DEBUG=*  # uncomment for verbose logging and set --logFile via client config if persistent logs are required
# Playwright MCP server (optional CLI flags such as --headless can be added in .mcp-config or client overrides)
# MarkItDown MCP server (install markitdown-mcp with desired extras; add env overrides here if needed)
# Model Context Protocol reference servers
MCP_FILESYSTEM_ROOT=/abs/path/to/project
MCP_GIT_REPOSITORY=/abs/path/to/project
